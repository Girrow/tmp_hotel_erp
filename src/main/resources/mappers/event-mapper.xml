<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.girrow.hotel.mapper.EventMapper">
	<select id="getAllEventList" resultType="EventVO">
		SELECT *
		FROM `h_event`
	</select>
</mapper>